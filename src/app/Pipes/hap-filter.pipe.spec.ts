import { HapFilterPipe } from './hap-filter.pipe';

describe('HapFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HapFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
